name: network
description: "A new Flutter package project."
version: 0.0.1
publish_to: none


environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  pretty_dio_logger: ^1.4.0
  dio_smart_retry: ^7.0.1
  local_storage:
    path: ../local_storage
  get_it: ^8.0.3
  dio_cache_interceptor: ^4.0.3
  crypto: ^3.0.6
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ">=2.12.2 <2.14.0"
  mockito: ^5.4.5